export interface Word {
  term: string;
  translation: string;
  explanation: string | '';
  training: boolean;
}

export interface PhrasalVerb {
  term: string;
  translation: string;
  explanation: string | '';
  training: boolean;
}

export interface Sentence {
  term: string;
  translation: string;
  explanation: string | '';
  training: boolean;
}
